<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="cartSQL"> 
	<insert id="insertCart">
		insert into cart values (#{c_cartId}, #{m_email}, #{p_code}, #{p_cost}, #{p_amount}, #{p_option1}, #{p_option2})
	</insert>
	
	<update id="updateCart">
		update cart set p_amount = p_amount + #{p_amount} where m_email=#{m_email} and p_code=#{p_code}
	</update>
	
	<select id="countCart" resultType="int">
		select count(*) from cart where m_email=#{m_email} and p_code=#{p_code}
	</select>
	
	<select id="cartList" resultType="cartDTO">
		select 
		c.cartId as cartId,
		c.m_email as email,
		p.p_code as p_code,
		m.m_name as m_name,
		p.p_name as p_name,
		c.p_amount as p_amount,
		p.p_cost as p_cost,
		(p_cost * amount ) money
		
		from 
		members m, product p, cart c,
		
		where m.m_email = c.m_email 
		and p.p_code = c.p_code
		and c.m_email = #{m_email}
		
		order by 
		c.c_cartId
			
	</select>
	<!-- 장바구니 total 금액 -->
	<select id="sumMoney" resultType="int">
		select NVL(SUM(p_cost * p_amount),0) money 
		from cart c, product p
		where c.p_code = p.p_code and c.m_email=#{m_email}
	</select>
	<!-- 장바구니 수정 -->
	<update id="modifyCart">
		update cart set p_amount=#{p_amount} where m_email=#{m_email} and p_code=#{p_code}
	</update>
	<!-- 장바구니 삭제 -->
	<delete id="deleteCart">
		delete from cart where c_cartId = #{c_cardId}
	</delete>
</mapper>